#include <iostream>
#include "polyomino.h"

int main() {
  Polyomino p;

  p.addCube();

  /**std::cout << p.getCubeCount() << std::endl;
  p.addCube();
  std::cout << p.getCubeCount() << std::endl;**/

  return 0;
}
